import { TimerDigitsPipe } from './timer-digits.pipe';

describe('TimerDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new TimerDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
